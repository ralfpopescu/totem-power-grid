import React from 'react';
import styled from 'styled-components';

import Page from '../../../Page';

const { Text, Container } = Page;

const OverviewPage = () => (
  <Container>
    <Text>
      Fields produced by totems can combine with other fields to create different fields or change the configuration of surrounding fields.
    </Text>
  </Container>
);

export default OverviewPage;