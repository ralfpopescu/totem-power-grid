import React from 'react';
import styled from 'styled-components';

const IntroPage = () => (
  <div>
    There are 5 kinds of totems: fire, water, earth, electric, and light. Each one has a unique effect on the tiles surrounding it.
    Place a totem on the grid by click a tile. Click the totem to remove it.
  </div>
);

export default IntroPage;